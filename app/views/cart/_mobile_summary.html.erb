<div class="fixed bottom-0 left-0 right-0 z-40 bg-white border-t-2 border-pink-200 p-4 shadow-2xl md:hidden" id="mobile-summary">
  <div class="flex items-center justify-between gap-4">
    <div>
      <p class="text-xs text-gray-600">Total</p>
      <p class="text-2xl font-bold text-pink-600">$<%= sprintf("%.2f", calculator.total_price) %></p>
    </div>
    <%= link_to "Checkout", checkout_path, class: "btn-primary flex-1" %>
  </div>
</div>
