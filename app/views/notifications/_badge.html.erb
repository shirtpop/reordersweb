<span id="notification-badge">
  <% if count.positive? %>
    <span class="absolute -top-1 -right-1 flex items-center justify-center min-w-[1.1rem] h-[1.1rem] px-1 text-[10px] font-semibold text-white bg-pink-600 rounded-full">
      <%= count > 9 ? "9+" : count %>
    </span>
  <% end %>
</span>
